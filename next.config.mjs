/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      "images-cdn.ubuy.co.in",
      "www.cnet.com",
      "a.img.cnet.com", 
    ],
  },
};

export default nextConfig;
